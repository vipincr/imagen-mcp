# Imagen MCP Server Dependencies
# 
# This project uses minimal dependencies to ensure broad compatibility.
# The only external dependency is FastMCP for the MCP protocol implementation.
# All other functionality uses Python standard library.

fastmcp>=2.0.0
